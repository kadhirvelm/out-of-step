export enum Routes {
    HELP = "/help",
    LIMIT_ORDER = "/limit-order",
    PORTFOLIO = "/portfolio",
    SCORE = "/score",
    STOCK = "/stock",
    STOCK_INFORMATION = "/stock-information",
    TRANSACTIONS = "/transactions",
    USER = "/user",
}
