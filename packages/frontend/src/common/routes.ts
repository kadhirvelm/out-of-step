export enum Routes {
    HELP = "/help",
    PORTFOLIO = "/portfolio",
    SCORE = "/score",
    STOCK = "/stock",
    STOCK_INFORMATION = "/stock-information",
    TRANSACTIONS = "/transactions",
    USER = "/user",
}
